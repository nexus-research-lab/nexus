# Computer Use settings

- This section controls the Nexus-owned package and sidecar lifecycle; it never accepts or displays endpoint, token, artifact-root, executable-path, manifest URL, or raw protocol input.
- The owner preference is the authorization switch. Disabling it must take effect before any best-effort sidecar stop.
- Browser is independent. Do not infer, mutate, or require Browser/CDP state from this section.
- Keep alpha/readiness wording honest: package health is not native macOS/Windows release acceptance.
