# 反馈展示原语

- `feedback-banner-model.ts` 用 tone 定义表统一图标、时长和视觉类名。
- `feedback-banner.tsx` 只渲染单条反馈并管理该反馈的自动消失计时。
- `feedback-banner-viewport.tsx` 提供固定定位边界；当前产品只有单条反馈，不维护伪堆叠协议。
- 业务域负责生成标题、消息和关闭命令；共享层不解释业务反馈类型。
- 消息内容变化必须重置自动消失计时，不能沿用上一条反馈的生命周期。
