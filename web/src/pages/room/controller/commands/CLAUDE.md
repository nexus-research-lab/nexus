# Room 页面命令

- 所有命令使用控制器已绑定的 `roomId`，消费者不得重复传入作用域标识。
- 写操作无论成功或失败都按命令策略读取一次权威快照；运行时关闭不触发无意义的完整资源刷新。
- Room 管理提交通过一个复合命令保持“补成员、更新设置、移除成员”的依赖顺序，整个提交只刷新一次。
- Agent 设置保存复用 Agent Options 领域命令，Room 命令只负责保存后的成员上下文刷新。
