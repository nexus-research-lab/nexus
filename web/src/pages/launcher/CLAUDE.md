# Launcher 页面

- 页面只装配共享目录、Console 与导航失败投影；真实 DM 身份由 direct-room navigation 解析。
- 每次用户选择建立导航代次；过期请求或卸载后的完成不得修改 Agent、侧栏或路由。此代次仅隔离页面投影，不取消或重放已受理的 ensure 操作。
- 只有明确 not_applied 的失败允许用户重试，结果未知继续走既有目录核对入口。
