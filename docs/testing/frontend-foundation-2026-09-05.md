# 前端基础重构验收记录 · 2026-09-05

**Non-normative。** 本文记录本次本地交付与验证边界，不新增实现规则。后续 Agent
从 [`web/AGENTS.md`](../../web/AGENTS.md) 进入唯一
[工程规范](../specs/frontend-engineering-spec.md) 与 [设计规范](../../design.md)。

## 已落地

- 先统一实现和所有权，再调整共享视觉。路由合同、通用 React/browser 能力、认证
  身份投影不再依赖 App；Markdown 通过中立能力注入接收文件动作，业务 mention/
  Slash 与 Agent scope 留在业务层；受控会话标签只处理展示和滚动，Room 命令归导航功能。
- Select 单选/多选触发器、Dialog 关闭、标签关闭复用唯一原语；登录页使用公共表单、
  按钮、排版和恢复提示。旧迁移路径与兼容转发已移除。
- 浮层只在当前模态范围内逐层退出；嵌套 Portal、禁用选择器、菜单初始焦点、隐藏
  Mention 的键盘监听均有回归测试。移动端会话切换器接入共享模态行为。
- 公共 Button 的默认高度、字重、图文间距和不可用/忙碌/hover 状态归共享样式所有者；
  Form/Select 的默认字号、行高和字段高度对齐，Select 标签不再覆盖加粗。
- TypeScript AST 依赖门禁不再保留反向依赖白名单；L3 合同检查递归覆盖已治理目录，本批扩展到 List 与 Workspace Catalog。
  业务层 13 个原生按钮几何所有者、17 处原生节点只在工程规范登记，门禁同时检测
  JSX 与直接 `createElement`。这不是允许继续复制按钮的额度。
- 对本次新增生产模块的 23 个导出符号做编译器引用审计，均有外部引用；新文件没有
  留下无调用者的兼容入口。此结论不冒充整个仓库所有导出的清理结果。
- Windows 原生浅色主题增加逐个语义 Brush 与阴影颜色的 token 投影门禁；原生主题
  变更也会触发前端 CI，避免手工维护的颜色与 Web 真相源分叉。
- 初次全前端编译器扫描覆盖 1,273 个模块、3,830 个导出，核对完全无引用运行时候选的
  动态测试用途后，移除 15 个未使用运行时导出及其失效状态；引导页 Note 迁入公共 Notice
  后再删除其样式入口，共清理 16 个运行时导出与 3 个孤立类型。类型合同与动态加载的纯模型测试入口不能
  仅因静态外部引用为零而删除，扫描结果不等于所有候选都是死代码。
- 从 Web、三个桌面入口和开发 Gallery 做模块可达性追踪（含静态、动态和类型导入），
  剩余两个独立文件分别是 Vitest setup 与用户原有未跟踪的 `lib/room-events.ts`；
  前者由测试配置加载，后者保留用户工作区状态。未发现本次提交新增的孤立生产模块。
- Contacts 与 Skill 的整卡点击层归 `WorkspaceCatalogCard.primaryAction`；共享局部
  堆叠使静态内容透传命中，次动作仍独立。ListAction 删除自身按钮 DOM 与重复状态
  配方，组合 IconButton，并统一侧栏、固定项和 Provider 条目的 hover/focus 展示。
- Select 的紧凑筛选与大号表单通过 `size` 选择；普通按钮与选择器的局部颜色、hover、
  字重、阴影覆盖已收口。新增 AST 门禁覆盖导入别名、命名空间、常量作用域、展开
  Props 和静态内联样式，当前无违规白名单；运行时或外部 CSS 仍需人工审查。
- 表单门禁扩展到 Input/Textarea/NativeSelect/SearchInput/Checkbox/Choice，包括搜索
  内部输入；移除普通字段的局部圆角、字重、行高，路径/命令/源码模板与验证码
  由共享 `textRole` 表达差异。角色只拥有展示，不猜测业务格式或修改原生输入值。
- ListRow 将侧栏和加载骨架的响应式尺寸、连续/独立边界和弱化展示收口为语义属性；
  静态/禁用行不再显示 hover。飞书连接入口使用真正的 disabled 状态保留行的名称
  和按钮语义并阻止重复命令。原生 Button/Select 检查共用 AST，识别 React 工厂别名，
  不把注释和源码示例中的标签当作实际 DOM。
- Agent 高级授权行复用 ListRow、图标框和 Typography；仅开关触发命令，已选的
  失联 Connector 仍可撤销。Skill 卡片使用 Catalog 的 dense 密度，锁定与提交中
  状态保持原边界。目录创建入口删除独立按钮实现，复用 Button 的焦点与禁用状态。
- Composer 附件的图片/文本预览和移除共三个原生按钮迁入共享 Button；保留 48px
  缩略图几何，移除统一使用 micro IconButton。Chip、Composer 与消息编辑器删除
  重复圆角和无效历史 class，只由共享输入壳/Chip recipe 持有表面。
- 四档控件与四档表面圆角集中为 token，Input/Chip/Popover/Dialog/Tooltip 等 recipe
  和内容别名统一消费。修复 Tour 缺失圆角及 Memory、Loop、目录侧面、附件文字等
  未定义颜色引用，直接选择现有语义，不新增兼容别名。
- 新 token 门禁使用 CSS parser 与 TypeScript AST 检查静态引用、模板 CSS、注释、
  字符串内容和嵌套 fallback，并逐主题检查 canonical 别名缺失、循环与块内重复。
  运行时表达式、DOM 继承和属性类型仍需实际 Surface 审查，声明集合不能代替渲染。
- WorkGraph 节点/连线详情归并为唯一领域检查器，复用公共浮层、排版和关闭按钮；
  精确选择、图布局和平移/逆缩放仍由画布拥有。生命周期、部分投影和旧快照提示
  改用公共 Badge 的语义属性，删除各自的徽标颜色配方。
- Login 与 Setup 统一品牌背景、入口标识、宣传标题和响应式布局；并排初始化字段
  保留较宽表单列。普通标题/说明使用 Typography，表单共同选择 Panel `filled`，
  由现有面板 token 减弱装饰背景穿透，删除 Setup 私有玻璃材质和 Login 重复 Logo 配方。

- 私域预览/时间线共用 filled Panel，名称/时间/路由、消息尾部元信息和 WorkGraph
  运行活动统一使用公共排版与内容行。方向气泡、正文编辑、身份叠放及图形微标签
  保留有明确所有者的几何，未改变 Markdown 的 exact 来源 Agent 或图拓扑。
- 原位消息编辑增加中立 IME 事件保护；普通 Enter、取消、空值/未变化抑制与
  exact round 提交保留。Composer 复用同一识别，保留自身 composition 生命周期
  与 Safari 时序；输入间距移回视图 recipe，光标/聚焦 DOM 操作移入控制器适配。

- User/Assistant 的正文尺度与外侧节奏归并到唯一阅读布局；角色专有的头部、数学
  正文间距和折叠测量继续由该布局表达，数据投影不再携带 CSS。队列 padding 回到
  Composer 样式所有者，File Artifact 的身份/资格与几何分离，不保留兼容转发。
- 文件默认标签与打开提示接入双语目录；预览/下载保持独立，切换全局 Agent 后仍
  绑定原文件作用域。新增 DOM 回归覆盖该边界及 Goal 控制记录的不可编辑语义，
  普通同名 Slash 正文继续保留普通消息动作。
- 新增 `make app-check-ui`：以独立 QA App 编译当前生产窗口、WKWebView 和 bridge，
  直接加载真实 Gallery。测试有独立应用标识、偏好、状态根、端口和 Vite 缓存，
  不启动产品 AppDelegate/sidecar，测试服务拒绝业务 API；脚本自动记录源码清单、
  原生输入证据、窗口指标、恢复事件及截图，并在退出时关闭自己启动的进程。

## 本地验证

### 分类筛选、上下文提示与冗余清理补充

- Skill、Connector、Channel、Pairing 等目录共用必填文字标签的
  `CapabilityFilterSelect`；移除页面自选的前导筛选图标参数，保留筛选值、
  搜索命令、来源选择及按内容调整的宽度。Gallery 新增真实产品控件夹具。
- 首轮截图发现普通英文分类值也被旧 144px 宽度截断；默认宽度统一为 176px，
  删除来源控件已有的同值覆盖，并增加真实标签不截断的浏览器断言。
- Composer 上下文入口显式关闭 IconButton 自动 Tooltip，DM 和逐 Agent 的
  Room 用量只保留同一份详情。真实浏览器回归发现 hover 后 Escape 会因恢复
  焦点重开提示，现由共享浮层的只读焦点策略修复；输入框保留焦点，键盘已在
  触发器上时也保持原位。菜单和 Dialog 的焦点归还仍由原合同及嵌套测试验证。
- 编译器扫描覆盖 1,284 个模块、3,820 个导出，并用全文引用与动态 SSR 测试
  交叉核对候选。移除 9 个无调用者的手写类型（含连带失效的旧 Skill 响应）、3 个过期 helper，将详情 Header
  收为内部函数；不按静态零引用删除生成协议或真实动态测试入口。
- 清理后再次扫描 1,284 个模块、3,806 个导出，`src/types` 中没有内部和外部
  消费者都为零的手写类型候选；这不等于证明整个前端不存在传递死代码。
- 时间线窗口断言改测当前 `buildIndexedConversationWindow`；失败恢复改测
  当前 `latestAssistantResultFailure` 的精确身份与分类，仍覆盖新轮抑制旧错、
  部分输出和 Room 同轮失败。删除从未连接生产资源的 Room activity key 及其
  无效刷新断言，保留精确停止/ACK 竞争测试与当前 Session invalidation 合同。
- 已人工复核英文桌面、中文深色/雨天窄屏筛选与 DM/Room 详情截图：统一箭头与
  分隔结构，普通英文值完整可见，只显示一份用量面板。截图为当前场景证据，
  自动检查另覆盖延迟提示、逐层 Escape、焦点和视口边界。

以下表格记录最新前端检查。macOS 组件宿主与真实 App 壳层分别验证，
不把 Gallery 结果作为真实页面或全部聊天业务的验收替代。

| 检查 | 结果 |
| --- | --- |
| ESLint、TypeScript、生产构建 | 通过 |
| 纯模型、协议与架构合同 | 454 项通过（含 token 完整性、原生投影、Access 所有权、列表/表单、导航持久化与 QA 接口隔离门禁） |
| Vitest 组件与资源行为 | 149 个文件、364 项通过 |
| Chromium | 三主题 × 双语 × 320/767/768/1440px，504 项通过 |
| WebKit | 三主题 × 双语 × 320/1440px，252 项通过 |
| Windows 浅色 token 投影 | 新增合同通过；只检查源码颜色映射 |
| macOS 宿主生产代码 | `swift build --package-path desktop/macos` 通过 |
| macOS 原生 WKWebView 组件宿主 | 三主题 × 双语 × 360/1280pt，12 组原生交互通过 |
| macOS 真实 Launcher / App Router | 三主题 × 双语 × 360/1280pt，12 组原生输入、窗口缩放与隐藏恢复通过 |

浏览器使用 Playwright 1.63.0、独立本地 Vite 和真实组件，重试为零。
登录测试完全拦截认证接口，不接触真实账号或后端数据。几何、键盘、焦点、碰撞、
滚动、模态层级、输入/清除/选择与 reduced motion 都通过真实浏览器观察。
macOS WebKit 使用宿主的 Option-Tab 全控件遍历规则，模态焦点链通过键盘触发验证。

截图随 `web/playwright-report/` 输出，失败证据位于 `web/test-results/`；生成物不提交。
已人工复核窄屏字段、标签、登录页、雨天弹窗与桌面按钮状态。字段按各自可见范围
截图，避免超出 Gallery 内部滚动视口后出现空白图片。
目录卡片新增真实坐标命中与主次命令隔离，列表动作新增键盘显示与禁用 hover 验证；
已复核浅色桌面、深色窄屏、WebKit 雨天窄屏的卡片焦点和列表动作截图。
表单角色新增原生提交值测试，覆盖技术标识、多行文本、验证码长度与前导零；
已复核浅色桌面技术字段、WebKit 雨天窄屏源码模板与验证码截图。
列表覆盖共享侧栏密度的真实高度/圆角、连续行零圆角和静态/禁用 hover，已复核
浅色桌面与 WebKit 雨天窄屏的行几何截图；飞书入口通过 busy 前后真实点击与键盘
事件验证可访问名称、重复命令阻断及恢复后的手动分支。
Agent 高级设置夹具验证权限行/Skill 的唯一开关命中、失联授权撤销、锁定与提交中
状态；目录创建入口验证原生表单类型、键盘命令与禁用 hover。已复核浅色桌面和
WebKit 雨天窄屏的权限行、Skill 卡片与禁用创建入口截图，长页面逐行滚动可达。
Composer 附件验证本地图片与文本解码、长正文换行、预览/移除独立命中、键盘焦点
归还、Session 重置与 Object URL 释放；已复核浅色桌面和雨天窄屏的附件、图片与
文本预览截图。焦点归还继续遵循工程规范中的宿主原生鼠标/键盘差异。
Tour 夹具包含真实目标；已复核浅色桌面与 WebKit 雨天窄屏的步骤卡片和 10px
高亮圆角截图，验证目标外扩、Escape 关闭、目标点击透传和单次命令。三主题另
检查当前匹配根元素的 CSS 变量没有空解析结果；此结论不等于每个业务页面已截图。
WorkGraph 的真实画布夹具覆盖节点/连线切换、exact Run 引用和原 Agent 文件打开，
验证关闭按钮与键盘 Escape、缩放后的屏幕宽度/字号及公共表面。已复核浅色桌面
节点详情与 WebKit 雨天窄屏连线详情截图；长详情仍允许在画布及面板内滚动。
Login/Setup 共享品牌尺度由真实路由矩阵验证。Setup 全部业务请求被拦截，检查
字段校验、键盘顺序、pending 禁用、精确 Bearer capability 与请求正文、失败后
仅重读状态、未开放状态及凭证不进入浏览器存储；无真实账号或部署发生变化。
已复核浅色桌面的 Login/Setup 宣传区以及 WebKit 雨天窄屏 filled 表单；填充减弱
背景网格，主题自身的雨滴视觉保留。截图只证明这些实际场景，不充当像素基线。

私域与消息编辑新增真实视图矩阵，覆盖 compact/regular 的方向和元信息、正文编辑
聚焦/高度、IME 期间不发命令、普通 Enter、取消重置与 exact round 的唯一提交。
WorkGraph 另验证静态运行活动无按钮，以及缩略图的节点顺序、依赖层次与只读语义。
已复核桌面运行活动/缩略图和 WebKit 雨天窄屏时间线/编辑器截图。

首次并行验证中，Vite 对懒加载 Markdown 依赖返回 `504 Outdated Optimize Dep`，
触发错误边界重建页面；一个合同测试进程也曾因 `SIGBUS` 中止。浏览器服务器现以
`browser-test` mode 使用独立优化缓存，避免与开发及 SSR 合同服务器共用缓存。
配置解析合同证明缓存目录隔离；同样并行重跑后，完整前端检查和 684 项浏览器测试
全部通过，零重试。未修改断言来忽略元素脱离或应用加载失败，`SIGBUS` 本身的根因
未单独定位；这些迭代中的失败不作为业务行为回归通过的证据。

本轮阅读布局矩阵进一步核对真实 User/Assistant 紧凑正文 14/24px、展开正文
16/28px 及一致的外侧间距，确认布局归并没有改变阅读尺度；两种密度的文件卡片
保留原 Agent 打开，并在中英文窄屏中不产生横向溢出。已复核桌面与 WebKit 雨天
窄屏截图；文件卡片的原生命中区和独立外部按钮数量保持既有合同。

原生批次在 macOS 26.6.2 / arm64 上从当前源码编译，完整矩阵验证 AppKit 点击、
文本输入、Return/方向键/Option-Tab/Escape，DOM 观察到的输入事件均为 trusted。
按钮保持 36px、14px/500、8px 图文间距，输入框保持 36px、14px/400；Select 在
视口底部向上展开，禁用项跳过，嵌套 Portal 位于模态命中层之上，逐层关闭归还焦点。
每个窗口隐藏超过宿主既有的五秒探针合并间隔后恢复，均得到新的 ready 探针，
文档标记保留且未触发重载。原生红灯中心为 (16, 24)pt，窗口按钮安全区为 85pt。
全部场景对业务 API 的请求数为零；产物保存在 `/tmp/nexus-native-ui/<run>/`。
已复核浅色桌面弹窗、深色窄窗选择器和雨天中文窄窗弹窗，浮层、关闭与 Footer
动作均可见；36 张 WKWebView 截图作为场景证据，不作为像素基线。

首轮驱动曾读错 Gallery 数据属性；随后测量又读到远端字体 CSS 尚未完成时的默认
控件样式。夹具已沿用浏览器测试的远端字体阻断，并等待页面和本地字体就绪。
截图另等待有限动画与绘制帧，避免在浮层透明首帧取证；这些修正只影响验收程序。
本批重跑 447 项合同，生产前端没有新增修改，组件/构建及 684 项浏览器结果沿用
上一批已验证的生产源码基线，不把原生组件矩阵当作全部浏览器或业务验收的替代。

## 真实 App 壳层跟进

新增真实 `app.html`、Launcher 与 App Router 的浏览器和原生套件。固定读取由同一
夹具拥有，空闲事件连接只支持 ping 与订阅；原生 Vite 的 HTTP/WS 后端代理全部
禁用。独立合同用真实本地哨兵验证读请求、拒绝写入/未知请求、拒绝畸形 WS 消息，
并证明后端收到的请求数为零。测试不依赖真实账号，不启动产品 sidecar。

首轮当前源码原生 App 冒烟通过 Launcher 输入、进入工作台与 360/1280pt 响应式
切换，记录于 `/tmp/nexus-native-ui/a4d2c080ba8741e0b7fb741bfabb9dc4/`。
该截图定位到英文一级导航被截断，随后修正共享轨道和动作的可用宽度，保持原图标
几何和路由命令；同步删除规范中的过期折叠宽度描述。浏览器冒烟已验证浅色英文
桌面与雨天中文窄屏的标签、往返导航、页面边界及零业务写入，播放器使用已安装
依赖中的 WASM，避免 CDN 可用性影响页面检查。

固定持久化修复前的完整浏览器批次共 756 项通过，零重试。导航最终按用户反馈采用清楚的短标签，
英文为 Chats / Agents / Library，页面完整领域名保持不变；中英文共用 64px
轨道、图标和字号，避免为了全名扩大导航。对应引导文案同步使用可见入口名。
标签检查按实际文字矩形核对内容区，取消整像素宽度容差，避免不足 1px 的裁切
也触发省略号而被门禁漏过。
最终标签和宽度随后跑完 36 组真实 App 浏览器检查。中英文桌面截图已人工复核，
短标签完整，没有按语言复制组件或建立不同轨道宽度。

随后用隔离 WebKit 页面复现固定丢失：页面 A 新固定会话，页面 B 保存自己的
Room 导航时把整份旧固定列表写回；A 内存仍显示固定，刷新才暴露丢失。普通
单页面同 owner 刷新保留固定，说明复现中的刷新只是读取已被覆盖的记录。
修复由导航 Store 在保存前合并同 owner 的最新持久快照，并通过存储失效信号
同步其他页面；App 注入身份校验，快照携带 owner 绑定，旧页面不能写入或读取
新账号的导航。无变化命令保持引用，避免持久恢复触发导航渲染循环。
六项新增合同覆盖旧页面保存、显式取消固定、刷新、无回写同步、状态引用和
跨 owner/旧版本恢复；与五项认证隔离合同联合通过。另修复迟到身份失效通知
会写空新账号快照的问题：失效只清本页，后续权威绑定恢复同 owner 快照，确认
登出仍清除持久记录。该回归先验证失败，再由 Store 和 Auth 集成合同验证修复。

固定保存主修复完成后，完整浏览器矩阵 756 项通过、零重试。最后的身份失效调整
再次通过全部 144 项 App/登录/初始化场景，其余 612 项 Gallery 对应的生产实现
没有改变，沿用完整批次结果。App 检查明确等待两个页面都显示固定后再取消，
验证刷新保留、跨页移除同步和再次刷新不复活；中英文 64px 导航及刷新后的固定
截图已人工复核。完整批次报告另存于 `/tmp/nexus-pin-full-browser-baseline/`，
最终受影响场景报告位于 `web/playwright-report/`，均为本地测试证据。
最终 `npm run check` 通过：lint、typecheck、454 项合同、149 个文件的 364 项
组件测试及生产构建。导航组件夹具显式绑定测试 owner，继续验证精确固定、
禁用固定、关闭后的相邻选择和新建事务，未放宽既有业务断言。

2026-09-06 解锁后，`make app-check-ui-app` 在当前生产窗口与真实 `app.html`
上完成三主题、双语及两种窗口宽度的全部 12 组检查，退出码为 0。证据位于
`/tmp/nexus-native-ui/d99070e14c1848fd992cf8382139202c/`，包含成功报告、
24 张截图及 1,741 个源码文件的 SHA-256 清单；运行后核对源码无差异。
实际 AppKit 点击、键盘和输入事件均为 trusted，页面错误与夹具拒绝记录均为空。
64px 一级导航的所有标签通过真实文字矩形检查；360/1280pt 往返调整与隐藏超过
五秒后的 ready 探针恢复均保留原文档。双击标题区的窗口宽度分别从 360/1280pt
变为 1512pt，再精确恢复原始宽高，报告保留三次实际尺寸。已人工复核浅色英文
桌面工作台、深色中文窄屏工作台和雨天中文窄屏 Launcher，导航与主要入口可见。

此前锁屏失败批次不作为通过证据。解锁后的首轮在中文浅色窄窗的第二次双击时
未恢复窗口，记录于 `/tmp/nexus-native-ui/6a91caf0dcb4469bb9dc142269ecb884/`。
驱动原来只等待 AppKit 窗口尺寸变化；现通过 QA 宿主的只读探针等待生产宿主
确认目标位置可拖动，再发送原生双击，避免尚未同步的 WebKit 下一帧拖动区域
影响手势前置条件。该修正只涉及验收程序，不更改生产手势实现，不重试手势，
也不放宽恢复尺寸断言；随后的完整 12 组均通过。

## 验证边界与后续迁移

- GitHub Actions 已配置同一套门禁和证据归档；本次只做本地提交，没有推送或运行远端 CI。
- Chromium/WebKit 覆盖 Web 渲染和输入行为；独立 macOS QA App 补充当前 WKWebView
  组件输入，以及真实 Launcher/工作台、原生窗口指标、双击缩放和隐藏恢复。
  WKWebView 截图不含系统 chrome，原生按钮位置另由 AppKit 测量；真实聊天后端
  读写、鼠标拖动窗口位置及发布安装流程不在该隔离套件范围内。
- 早期原生检查受 Mac 锁定限制；WorkGraph 批次已能打开 Nexus 的 Launcher 和工作台。但已安装
  App 在 34343 返回 `app-dr89c6zo.js`，当时本地构建为 `app-Bkvi2xn1.js`，因此此次
  窗口观察不作为当前修改的 WKWebView 验收。未替换用户正在运行的 App。本机 Command
  Line Tools 缺少 XCTest，窗口指标测试编译失败（`no such module 'XCTest'`），生产
  Swift 编译通过。上述历史限制不覆盖后来成功完成的独立原生 App 套件。
- 用户在 2026-09-06 明确要求本次先不验收 Windows，优先完成前端重构。
  因此 Windows 虚拟机检查已停止，WPF/WebView2 实际运行保持未验收；源码 token
  投影合同通过不等于原生行为通过。本次范围调整不修改后续跨平台交付的一般规范。
- 产品 `SidecarPortAllocator` 仍固定使用 34343；QA 通过显式构造独立 runtime 复用
  生产窗口，已绕开组件宿主验收的端口冲突，没有修改产品端口或替换用户运行的 App。
- 本地字体用于 App chrome 验收，远端聊天中文字体被显式阻断；没有建立跨平台像素基线，
  也没有声称所有业务页面和全部阅读 Surface 均已视觉验收。
- `hooks/store/types/lib` 的剩余目录整理继续按领域切片渐进迁移；当前严格依赖门禁与
  公共所有者合同已经生效，不能因目录尚未整体移动而恢复反向依赖或私有普通控件。
- 工作区原有改动经用户授权纳入同批本地提交：包含空会话欢迎区居中与中文
  名称间距、对应组件测试、明确标记 non-normative 的 WorkGraph 探索稿，以及
  已有后端整数 token 修复的 CHANGELOG 补记。它们一直参与本次完整前端检查。
  唯一无引用的未跟踪 `lib/room-events.ts` 按冗余清理要求删除，不作为兼容入口保留。
  删除后类型检查通过；生产与开发入口的可达性扫描仅剩由 Vitest 配置加载的测试 setup。

## 本次完成审计（non-normative）

已清理本次反向扫描定位的 Composer/队列 padding、File Artifact 密度投影及
User/Assistant 消息布局混合职责，并将六个相关模型纳入同一“无 CSS/DOM 副作用”
门禁。文件布局字段均有实际消费者，未发现可直接删除的残留字段。

`document-preview-model.ts` 的 `className` 属于 DOCX 第三方渲染选项，
`conversation-panel-model.ts` 则只是类型中的 `Omit<..., "className">`；不能按
字符串命中将它们认定为私有控件。任意圆角、色值和字号扫描仍需区分普通 UI 与
文档/品牌/图形数据，运行时表达式与外部 CSS 的证据边界保持不变。

| 用户要求 | 本次落地结果 |
| --- | --- |
| 后续 Agent 遵循唯一代码与 UI 规范 | `web/AGENTS.md` 指向唯一工程规范与 `web/design.md`；模块地图与文件契约只描述自身职责，产品规范保留领域语义 |
| 统一组件所有权和依赖方向 | 公共控件、布局角色与业务适配分层；AST 依赖门禁无历史豁免，覆盖静态、类型、动态导入和重导出 |
| 减少同类按钮和样式的私有实现 | 普通动作、表单、目录、列表与浮层使用公共所有者；原生按钮只保留规范登记的几何/语义例外，样式门禁阻止业务层覆盖公共控件 |
| 注释合同、token 与关键行为门禁 | 文件契约、三主题 token 完整性和引用、原生映射、组件行为与浏览器矩阵纳入本地检查及 CI 配置 |
| 渐进优化并清理冗余代码 | 归并布局与密度投影，删除已证实无消费者的运行时导出、类型和旧辅助入口；不以机械目录搬迁替代职责整理 |
| 修正截图反馈和刷新固定丢失 | 统一分类/状态筛选、仅保留一份上下文用量面板；64px 双语导航采用明确短标签；导航存储按 owner 合并并同步，覆盖刷新和跨页回归 |
| 完成本地交付 | 前端检查、完整浏览器矩阵及受影响场景复测通过；macOS 组件与真实 App 各 12 组原生检查通过；Windows 按用户要求暂缓，仅本地提交 |

本次重构与已反馈问题已完成。后续业务需求继续按现有所有权和门禁渐进迁移，
不把未修改目录的整体搬迁或用户暂缓的 Windows 验收扩大为本次交付前置条件。
